# Demo Repo

This is a tiny demo repository for the Repo-Task Runtime / Workbench.

Suggested task:

Fix the slug formatter so titles are normalized with hyphens instead of underscores, then run the local test suite.

Suggested verification command:

```bash
python3 -m unittest discover -s tests -v
```
